function clearInput1(){
  if(ol1.value() == " USER INPUT (Oligo1) only 'AUGC' characters allowed : "){
    ol1.value("");
  }
  if(ol2.value() == ""){
    ol2.value(" USER INPUT (Oligo2) only 'AUGC' characters allowed : ");
  }
  if(ol1Position.value() == ""){
    ol1Position.value("Oligo1 position");
  }
  if(ol2Position.value() == ""){
    ol2Position.value("Oligo2 position");
  }
}

function clearInput2(){
  if(ol2.value() == " USER INPUT (Oligo2) only 'AUGC' characters allowed : "){
    ol2.value("");
  }
  if(ol1.value() == ""){
    ol1.value(" USER INPUT (Oligo1) only 'AUGC' characters allowed : ");
  }
  if(ol1Position.value() == ""){
    ol1Position.value("Oligo1 position");
  }
  if(ol2Position.value() == ""){
    ol2Position.value("Oligo2 position");
  }
}

function clearInput3(){
  if(ol1Position.value() == "Oligo1 position"){
    ol1Position.value("");
  }
  if(ol1.value() == ""){
    ol1.value(" USER INPUT (Oligo1) only 'AUGC' characters allowed : ");
  }
  if(ol2.value() == ""){
    ol2.value(" USER INPUT (Oligo2) only 'AUGC' characters allowed : ");
  }
  if(ol2Position.value() == ""){
    ol2Position.value("Oligo2 position");
  }
}

function clearInput4(){
  if(ol2Position.value() == "Oligo2 position"){
    ol2Position.value("");
  }
  if(ol1.value() == ""){
    ol1.value(" USER INPUT (Oligo1) only 'AUGC' characters allowed : ");
  }
  if(ol2.value() == ""){
    ol2.value(" USER INPUT (Oligo2) only 'AUGC' characters allowed : ");
  }
  if(ol1Position.value() == ""){
    ol1Position.value("Oligo1 position");
  }
}